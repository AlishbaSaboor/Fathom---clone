# hook bookkeeping only; .agent-logs/ is intentionally NOT ignored
.claude/hooks/.state/
__pycache__/

# dependencies / build output
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
node_modules

# local env files hold secrets (API keys); never commit them.
# .env.example is the committed template with placeholder values only.
.env
.env.*
!.env.example

# dev server output when started detached (see .next-dev-logs/)
.next-dev-logs/
